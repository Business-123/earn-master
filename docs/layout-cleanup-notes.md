# Layout cleanup notes

This cleanup pass focused on the laptop-first admin shell:
- removed sidebar quick actions that duplicated topbar controls
- removed visible theme switching controls and forced the dashboard into dark mode
- removed the hamburger buttons from the visible layout
- made the page header non-sticky so only the topbar stays pinned
- reduced toast width and duration
- replaced the sidebar shield with an avatar
- tightened the topbar order and spacing
- softened the translucent overlay feel by making the shell surfaces more opaque
- removed the big empty record space where possible by shrinking placeholder containers and keeping record panels fluid
- kept the admin inspector as an on-demand overlay so it does not steal page width
- improved the dark palette toward a deeper graphite and indigo scheme
- kept the current admin feature set intact while removing clutter
